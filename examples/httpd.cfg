#
# this example shows use of openMarinkaRodeo's provisioning interface
#

# ------------------ module loading ----------------------------------

#set module path
mpath="/usr/local/lib64/openMarinkaRodeo/modules/"

loadmodule "db_mysql.so"
loadmodule "httpd.so"
  modparam("httpd", "port", 8888)
loadmodule "mi_http.so"
loadmodule "pi_http.so"
  modparam("pi_http", "framework", "/usr/local/src/openMarinkaRodeo/examples/pi_framework.xml")
loadmodule "mi_xmlrpc_ng.so"

# -------------------------  request routing logic -------------------

# main routing logic

route{
	exit;
}

